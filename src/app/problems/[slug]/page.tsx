import Link from "next/link"; import { notFound } from "next/navigation"; import { ArrowLeft } from "lucide-react"; import { getPrisma } from "@/infrastructure/database/prisma"; import { SubmissionForm } from "@/components/submission-form";
export const dynamic="force-dynamic";
export default async function ProblemPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const problem=await getPrisma().problem.findUnique({where:{slug,isPublished:true}});if(!problem)notFound();return <main className="page-shell"><Link className="back" href="/"><ArrowLeft size={15}/> All problems</Link><header className="problem-header"><div className="meta-row"><span>{problem.difficulty.toLowerCase()}</span><span>{problem.estimatedMinutes} minutes</span></div><h1>{problem.title}</h1><p>{problem.context}</p></header><section className="requirements"><p className="kicker">Requirements</p><h2>What the system must do</h2><ul>{problem.requirements.map(item=><li key={item}>{item}</li>)}</ul></section><section><p className="kicker">Your solution</p><h2>Make your reasoning visible</h2><SubmissionForm problemId={problem.id}/></section></main>}
