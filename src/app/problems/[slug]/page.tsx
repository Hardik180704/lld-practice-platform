import Link from "next/link"; import { notFound } from "next/navigation"; import { ArrowLeft } from "lucide-react"; import { getPrisma } from "@/infrastructure/database/prisma"; import { SubmissionForm } from "@/components/submission-form";
export const dynamic="force-dynamic";
export default async function ProblemPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const problem=await getPrisma().problem.findUnique({where:{slug,isPublished:true}});if(!problem)notFound();return <main className="page-shell"><Link className="back" href="/"><ArrowLeft size={15}/> All problems</Link><header className="problem-header"><div className="problem-copy"><div className="meta-row"><span>{problem.difficulty.toLowerCase()}</span><span>{problem.estimatedMinutes} minutes</span></div><h1>{problem.title}</h1><p>{problem.context}</p></div><aside className="brief-aside"><p className="kicker">Evaluation lens</p><ul>{problem.evaluationHints.map(hint=><li key={hint}>{hint}</li>)}</ul><small>There is no single reference solution. Make your decisions explicit.</small></aside></header><section className="requirements"><p className="kicker">Requirements</p><h2>What the system must do</h2><ul>{problem.requirements.map((item,index)=><li key={item}><span>0{index+1}</span>{item}</li>)}</ul></section><section className="solution-section"><div className="section-intro"><div><p className="kicker">Your solution</p><h2>Make your reasoning visible</h2></div><p>Write for another engineer reviewing your design. Specific examples beat generic principles.</p></div><SubmissionForm problemId={problem.id}/></section></main>}
